<%= form_for(@locacion) do |f| %>
  <% if @locacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locacion.errors.count, "error") %> prohibited this locacion from being saved:</h2>

      <ul>
      <% @locacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :imagen %><br />
    <%= f.text_field :imagen %>
  </div>
  <div class="field">
    <%= f.label :distancia %><br />
    <%= f.text_field :distancia %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
